// Stub for unused optional module
const React = require('react');
const { View } = require('react-native');
const Pass = (props) => React.createElement(View, props, props.children);
module.exports = Pass;
module.exports.default = Pass;
