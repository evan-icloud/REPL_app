/**
 * DocxEditor Component
 *
 * @eigenpal/docx-editor-vue 的React封装
 *
 * 功能特点:
 * - 支持10MB以上大体积Word文档导入
 * - 保留原排版格式
 * - 可直接复制到模板使用
 * - 支持富文本编辑
 */

export { default as DocxEditor, type DocxEditorProps, type DocxEditorRef } from './DocxEditor';
